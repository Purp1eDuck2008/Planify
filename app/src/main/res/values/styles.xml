<resources>
    <!-- Основной стиль приложения -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Установим цвет фона для приложения -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Прозрачный статус-бар -->
        <item name="android:windowTranslucentStatus">true</item>

    </style>
    <style name="AppDialogTheme" parent="Theme.MaterialComponents.DayNight.Dialog.Alert">
        <item name="colorPrimary">@color/divider_line</item>
        <item name="colorPrimaryDark">@color/divider_line</item>
        <item name="colorAccent">@color/divider_line</item>
    </style>

    <style name="PopupAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@anim/slide_out_left</item>
    </style>

</resources>
